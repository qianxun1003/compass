# 爬取大学官网数据可行性分析

## 📊 一、可行性评估

### ✅ 技术可行性：**可行，但有一定挑战**

爬取大学官网数据在技术上是可行的，但需要处理以下挑战：

### ⚠️ 主要挑战

1. **网站结构多样化**
   - 每个大学的网站结构不同
   - 信息可能分散在多个页面
   - 需要为每个大学编写特定的爬虫逻辑

2. **数据格式多样**
   - HTML页面
   - PDF文件（很多大学用PDF发布招生简章）
   - 表格数据
   - 动态加载的内容（JavaScript渲染）

3. **信息完整性**
   - 官网可能不包含所有需要的信息
   - 某些信息可能需要登录或特殊权限
   - 信息可能在不同年份的页面中

4. **法律和伦理**
   - 需要遵守 `robots.txt`
   - 需要遵守网站使用条款
   - 需要合理控制请求频率，避免对服务器造成压力

5. **维护成本**
   - 网站结构变化需要更新爬虫
   - 需要定期运行爬虫更新数据
   - 需要处理网站改版、页面失效等情况

---

## 📋 二、需要爬取的数据字段

根据当前系统需求，需要爬取以下数据：

### 基础信息
- ✅ 大学名称（`name`）
- ✅ 学部名称（`department`）
- ✅ 学科名称（`major`）
- ✅ 地理位置（`region`）
- ✅ 文理分类（`bunri`）

### 入试信息
- ✅ 入试方式（`selectionMethod`）
- ✅ 出愿期数（`period`）
- ✅ 是否可併願（`combined`）

### EJU相关
- ✅ 能使用的EJU时期（`ejuPeriod`）
- ✅ 需要的EJU科目（`ejuSubjects`）

### 语言要求
- ✅ 是否需要英语（`english`）
- ✅ 是否需要JLPT（`jlpt`）

### 时间信息（每年都会变化）
- ✅ 网上出愿开始时间（`mailStart`）
- ✅ 网上出愿截止时间（`mailEnd`）
- ✅ 邮寄开始时间（`mailStartDate`）
- ✅ 邮寄截止时间（`mailEndDate`）
- ✅ 邮寄截止说明（`mailEndNote`：必着/消印）
- ✅ 校内考时间1（`examDate`）
- ✅ 校内考时间2（`examDate2`）
- ✅ 发榜时间（`announcementDate`）

### 其他
- ✅ 校内考形式（`examFormat`）
- ✅ 特殊成绩要求（`specialRequirements`）

---

## 🎯 三、实现方案

### 方案A：全自动爬取（推荐用于试点）

**优点**：
- 完全自动化
- 可以定期更新

**缺点**：
- 开发成本高
- 维护成本高
- 需要处理各种异常情况

**实现步骤**：
1. 建立大学URL列表和页面结构配置
2. 为每个大学编写爬虫逻辑（或通用解析器）
3. 处理PDF文件（使用PDF解析库）
4. 数据清洗和格式化
5. 导出为JSON格式

### 方案B：半自动爬取 + 人工审核（推荐）

**优点**：
- 平衡自动化和准确性
- 可以处理复杂情况
- 降低维护成本

**缺点**：
- 需要人工审核
- 不能完全自动化

**实现步骤**：
1. 爬虫自动抓取基础信息
2. 生成Excel文件供人工审核和补充
3. 人工审核后导入系统

### 方案C：API集成（如果有）

**优点**：
- 最可靠
- 数据格式统一
- 维护成本低

**缺点**：
- 大多数大学不提供公开API
- 需要与大学协商

---

## 🛠️ 四、技术实现建议

### 推荐技术栈

```python
# 网页爬取
- requests：HTTP请求
- BeautifulSoup4：HTML解析
- Selenium：处理JavaScript渲染的页面
- Scrapy：大型爬虫框架（可选）

# PDF处理
- PyPDF2 / pdfplumber：PDF文本提取
- pdfminer：PDF解析

# 数据处理
- pandas：数据处理
- dateparser：日期解析（支持日文日期格式）

# 数据存储
- 临时存储：JSON / CSV
- 最终输出：与现有系统兼容的格式
```

### 爬虫架构建议

```
scripts/
├── crawlers/
│   ├── base_crawler.py          # 基础爬虫类
│   ├── tokyo_univ_crawler.py   # 东京大学爬虫
│   ├── kyoto_univ_crawler.py   # 京都大学爬虫
│   └── ...                      # 其他大学爬虫
├── parsers/
│   ├── pdf_parser.py            # PDF解析器
│   ├── html_parser.py           # HTML解析器
│   └── date_parser.py           # 日期解析器
├── utils/
│   ├── data_cleaner.py          # 数据清洗
│   └── export.py                # 数据导出
└── crawl_all.py                 # 主入口脚本
```

---

## 📝 五、实现示例代码

### 基础爬虫框架示例

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
大学官网数据爬取框架
"""
import requests
from bs4 import BeautifulSoup
from datetime import datetime
import json
from pathlib import Path
import time
import re

class BaseUniversityCrawler:
    """基础大学爬虫类"""
    
    def __init__(self, university_name, base_url):
        self.university_name = university_name
        self.base_url = base_url
        self.session = requests.Session()
        self.session.headers.update({
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
        })
        self.data = []
    
    def crawl(self):
        """主爬取方法，子类需要实现"""
        raise NotImplementedError("子类必须实现 crawl 方法")
    
    def parse_admission_info(self, html_content):
        """解析招生信息，子类需要实现"""
        raise NotImplementedError("子类必须实现 parse_admission_info 方法")
    
    def parse_date(self, date_str):
        """解析日期字符串为统一格式"""
        # 使用 dateparser 或自定义解析逻辑
        # 返回格式：YYYY-MM-DD HH:MM:SS
        pass
    
    def save_to_json(self, output_path):
        """保存为JSON格式"""
        output = {
            "university": self.university_name,
            "crawled_at": datetime.now().isoformat(),
            "data": self.data
        }
        Path(output_path).write_text(
            json.dumps(output, ensure_ascii=False, indent=2),
            encoding='utf-8'
        )
    
    def delay(self, seconds=1):
        """延迟请求，避免对服务器造成压力"""
        time.sleep(seconds)


class TokyoUniversityCrawler(BaseUniversityCrawler):
    """东京大学爬虫示例"""
    
    def __init__(self):
        super().__init__(
            university_name="東京大学",
            base_url="https://www.u-tokyo.ac.jp"
        )
        self.admission_url = "https://www.u-tokyo.ac.jp/admission/undergraduate/foreign.html"
    
    def crawl(self):
        """爬取东京大学招生信息"""
        try:
            response = self.session.get(self.admission_url, timeout=10)
            response.encoding = 'utf-8'
            
            if response.status_code == 200:
                soup = BeautifulSoup(response.content, 'html.parser')
                self.parse_admission_info(soup)
            else:
                print(f"无法访问页面: {self.admission_url}")
        
        except Exception as e:
            print(f"爬取失败: {e}")
        
        self.delay(2)  # 延迟2秒
    
    def parse_admission_info(self, soup):
        """解析招生信息页面"""
        # 这里需要根据实际页面结构编写解析逻辑
        # 示例：
        departments = soup.find_all('div', class_='department')  # 假设的CSS选择器
        
        for dept in departments:
            info = {
                "name": self.university_name,
                "department": dept.find('h3').text.strip(),
                "region": "東京都",
                # ... 其他字段
            }
            self.data.append(info)


def main():
    """主函数"""
    crawlers = [
        TokyoUniversityCrawler(),
        # 添加其他大学爬虫
    ]
    
    all_data = []
    for crawler in crawlers:
        print(f"正在爬取: {crawler.university_name}")
        crawler.crawl()
        all_data.extend(crawler.data)
    
    # 导出为与现有系统兼容的格式
    output = {"data": all_data}
    Path("crawled_school_data.json").write_text(
        json.dumps(output, ensure_ascii=False, indent=2),
        encoding='utf-8'
    )
    print(f"已爬取 {len(all_data)} 条数据")


if __name__ == "__main__":
    main()
```

### PDF解析示例

```python
import pdfplumber
import re

def parse_pdf_admission_info(pdf_path):
    """解析PDF格式的招生简章"""
    data = []
    
    with pdfplumber.open(pdf_path) as pdf:
        for page in pdf.pages:
            text = page.extract_text()
            
            # 使用正则表达式提取关键信息
            # 例如：提取日期
            date_pattern = r'(\d{4})年(\d{1,2})月(\d{1,2})日'
            dates = re.findall(date_pattern, text)
            
            # 提取EJU科目要求
            eju_pattern = r'EJU.*?科目[：:]\s*([^。\n]+)'
            eju_subjects = re.findall(eju_pattern, text)
            
            # ... 其他解析逻辑
    
    return data
```

---

## ⚠️ 六、注意事项和最佳实践

### 1. 遵守robots.txt
```python
from urllib.robotparser import RobotFileParser

def can_fetch(url, user_agent='*'):
    rp = RobotFileParser()
    rp.set_url(f"{url}/robots.txt")
    rp.read()
    return rp.can_fetch(user_agent, url)
```

### 2. 设置合理的请求间隔
```python
import time
import random

def delay(min_seconds=1, max_seconds=3):
    """随机延迟，模拟人类行为"""
    time.sleep(random.uniform(min_seconds, max_seconds))
```

### 3. 错误处理和重试机制
```python
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
def fetch_with_retry(url):
    response = requests.get(url, timeout=10)
    response.raise_for_status()
    return response
```

### 4. 数据验证
```python
def validate_school_data(data):
    """验证爬取的数据是否符合要求"""
    required_fields = ['name', 'department', 'region', 'bunri']
    
    for field in required_fields:
        if field not in data or not data[field]:
            return False, f"缺少必需字段: {field}"
    
    # 验证日期格式
    date_fields = ['mailStart', 'mailEnd', 'examDate']
    for field in date_fields:
        if field in data:
            try:
                datetime.strptime(data[field], "%Y-%m-%d %H:%M:%S")
            except ValueError:
                return False, f"日期格式错误: {field}"
    
    return True, "验证通过"
```

---

## 🎯 七、推荐实施方案

### 阶段1：试点爬取（1-2周）
1. 选择3-5所大学作为试点
2. 分析这些大学的网站结构
3. 编写爬虫脚本
4. 测试数据准确性

### 阶段2：扩展爬取（2-4周）
1. 根据试点经验，扩展更多大学
2. 建立爬虫配置系统（每个大学的URL和解析规则）
3. 实现PDF解析功能
4. 实现数据清洗和验证

### 阶段3：集成到现有系统（1周）
1. 修改 `export_school_data.py` 支持从爬取数据导入
2. 或者创建新的脚本 `import_crawled_data.py`
3. 实现数据合并逻辑（爬取数据 + 人工补充数据）

### 阶段4：自动化（可选）
1. 设置定时任务（如每周运行一次）
2. 实现数据变更通知
3. 建立数据质量监控

---

## 📊 八、数据质量对比

| 数据来源 | 准确性 | 完整性 | 更新频率 | 维护成本 |
|---------|--------|--------|---------|---------|
| Excel手动输入 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 手动 | 低 |
| 官网爬取 | ⭐⭐⭐ | ⭐⭐⭐ | 自动 | 高 |
| 半自动爬取+人工审核 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 半自动 | 中 |

---

## 💡 九、建议

### 推荐方案：**半自动爬取 + 人工审核**

**理由**：
1. ✅ 平衡了自动化和准确性
2. ✅ 可以处理复杂情况（PDF、特殊格式等）
3. ✅ 降低维护成本
4. ✅ 可以逐步扩展

**实施步骤**：
1. 先爬取基础信息（大学名、学部、位置等相对稳定的信息）
2. 时间信息（每年变化）可以爬取后人工审核
3. 生成Excel文件，与现有Excel格式兼容
4. 人工审核后，使用现有的 `export_school_data.py` 导入

### 不推荐：完全自动化爬取

**理由**：
1. ❌ 开发成本高（需要为每个大学编写特定逻辑）
2. ❌ 维护成本高（网站改版需要更新）
3. ❌ 数据准确性难以保证（特别是日期、特殊要求等）
4. ❌ 可能违反网站使用条款

---

## 🔗 十、相关资源

- [Scrapy文档](https://docs.scrapy.org/)
- [BeautifulSoup文档](https://www.crummy.com/software/BeautifulSoup/bs4/doc/)
- [pdfplumber文档](https://github.com/jsvine/pdfplumber)
- [日本大学招生信息网站示例](https://www.u-tokyo.ac.jp/admission/)

---

## 📝 十一、下一步行动

1. **确定目标大学列表**（优先爬取哪些大学）
2. **分析目标大学网站结构**（找到招生信息页面）
3. **编写试点爬虫**（选择1-2所大学作为试点）
4. **测试和优化**（验证数据准确性）
5. **扩展到更多大学**（逐步增加）

如果需要，我可以帮您：
- 编写具体的爬虫代码
- 分析特定大学的网站结构
- 设计数据导入流程
- 实现PDF解析功能
